<div>
  <dl class="dl-horizontal">
    <dt></dt>
    <dd>
    <h4>Author : <%= User.find_by(id: review.user_id).email %></h4>
    <h6>Product Rating : <%= review.rating %>/5</h6>
    <p>Review : <%= review.description %></p>
    <% if current_user.email == User.find_by(id: review.user_id).email %>
      <p><%= link_to 'Destroy', [@product, review], method: :delete %></p>
    <% end %>
    </dd>
  </dl>
</div>
